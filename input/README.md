# input/

Human-input devices: buttons, keypads, rotary encoders, joysticks, touch controllers. A touch controller physically on a screen still lives here, not in `display/`. Contains: BUTTON, JOYSTICK, KEYPAD.
