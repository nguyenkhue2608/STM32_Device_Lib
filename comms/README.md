# comms/

Communication and connectivity modules and protocols: RF (nRF24L01, LoRa), Bluetooth, Wi-Fi (AT modules), GPS, RS485/CAN transceivers, IR remote. An IR receiver is a protocol device and belongs here, not in `input/`. Application-level stacks (MQTT/TCP) belong in a separate repo.
